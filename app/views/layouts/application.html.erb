<!DOCTYPE html>
<html>
  <head>
    <title>Cachetons</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arvo:wght@700&family=Prompt:wght@300&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/2b29c26b4c.js" crossorigin="anonymous"></script>

    <%= javascript_importmap_tags %>
  </head>

  <body class="m-0 p-0">
    <% if params[:controller] == "pages" %>
      <%= render "shared/flashes" %>
      <%= yield %>
    <% else %>
      <div class="row m-0 justify-content-end">

        <div class="navbar_container col-12 col-md-4 col-lg-3">
          <%= render "shared/navbar"  %>
        </div>

        <div class="col-12 col-md-8  col-lg-9 m-0 p-4">
          <%= yield %>
        </div>

        <%= render "shared/flashes" %>

      </div>
    <% end %>
  </body>
</html>
