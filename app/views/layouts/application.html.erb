<!DOCTYPE html>
<html>
  <head>
    <title>Cachetons</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" unless params[:controller] == "devise/sessions" || params[:controller] == "home" %>
    <%= render "shared/flashes" %>
    <div class="rendered_page d-flex justify-content-between">
      <%= yield %>
    </div>
  </body>
</html>
